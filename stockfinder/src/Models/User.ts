export type UserProfileToken = {
    firstName: string;
    lastName: string;
    userName: string;
    email: string;
    token: string;
};

export type UserProfile = {
    userName: string;
    email: string;
}
